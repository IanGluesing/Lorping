# Test Scenarios

Each modulation scheme has six different automated test scenarios:

Non-Hopping: Ping, Curl, Audio
Hopping: Ping, Curl, Audio

Each modulation scheme has a main helper script that will build for a specific scenario and then perform the associated test